# DB-05.20.1 — Benchmark de generalização promocional

## Objetivo

Avaliar se o editor consegue representar uma peça promocional diferente do catálogo de fixação que originou grande parte do projeto, sem criar novos tipos de componente e sem editar JSON durante a construção.

Referência funcional: promoção de “Pé para Móveis”, com identidade, hierarquia promocional, argumentos, dados técnicos, quatro ofertas, desenho técnico, chamada de urgência, benefícios e rodapé.

## Política do teste

A criação e a edição foram realizadas por controles reais da interface em Chromium/Playwright:

- inserção pela biblioteca;
- edição pelo inspetor;
- entrada e saída de contextos internos;
- seleção pela página ou pela árvore de Camadas, conforme a semântica do componente;
- duplicação e distribuição pela interface;
- undo/redo pelos botões da toolbar;
- exportação JSON pela toolbar;
- reimportação pelo diálogo de importação;
- acionamento de Imprimir/PDF e geração de PDF A4.

A store foi usada somente para leitura, descoberta de IDs e métricas. Os três arquivos de arte permaneceram como placeholders porque o runner não recebeu assets comerciais.

## Execução final corrigida

- **Workflow:** `Developer B Generalization Benchmark`
- **Run:** `29883102904`
- **Resultado:** `pass-with-findings`
- **Artefato:** `developer-b-promotional-generalization-05.20.1`
- **Digest:** `sha256:9bcba49c8a3af9b86a57d66e93db1fa33ac740cca9c83e2a361ecdf9432a7cfe`
- **Duração:** 17,155 ms
- **Ações registradas:** 219

O workflow executa diretamente `tests/browser-promotional-generalization-05.20.test.js`; não há patch temporário ou job de escrita associado ao benchmark.

A execução anterior tratava o clique intencional no placeholder de arte como uma tentativa de seleção genérica. O DB-05.20.2 separou as duas ações: editar uma arte usa Camadas; clicar no placeholder abre a biblioteca de assets.

## Cobertura editorial

| Elemento da referência | Representação usada | Resultado |
| --- | --- | --- |
| Identidade / logo | `art` com `role=logo` | representado com placeholder |
| “Promoção da Semana” | receita `section-heading` | representado |
| Nome e argumento do produto | dois `text` | representado |
| Material | receita `fact` | representado |
| Capacidade de carga | receita `fact` | representado |
| Quatro alturas e preços | `product-card` em `variants` + `data-table` | representado |
| Imagem principal | `art` interna do card | representado com placeholder |
| Desenho técnico | `art` com `role=technical` | representado com placeholder |
| Chamada de urgência | receita `section-tip-callout` | representado |
| Quatro benefícios | `icon` duplicado e editado | representado |
| Contato e confiança | `catalog-footer` | representado |
| Faixas inclinadas, personagem e megafone | asset composto | não recriado com componentes nativos |

A estrutura relevante da promoção foi representada sem novo tipo e sem alteração de schema.

## Métricas estruturais

- schema: `1.16.0`;
- 59 componentes no modelo;
- 59 IDs únicos no modelo;
- 59 componentes no canvas;
- 59 IDs únicos no canvas;
- nenhum componente ausente no DOM;
- nenhum frame não finito;
- quatro linhas comerciais preservadas;
- rodapé em `x=24`, `y=1019`, `746×80`;
- zero colisões no relatório de publicação;
- zero overflows no relatório de publicação;
- uma referência ausente reportada pela publicação, ainda não diagnosticada;
- três artes sem asset, conforme a política do teste.

## Persistência, histórico e publicação

A exportação e a reimportação foram estruturalmente equivalentes. O documento reimportado preservou:

- 14 componentes de primeiro nível;
- 59 componentes totais;
- as quatro ofertas;
- as quatro receitas/papéis editoriais (`section-heading`, dois `fact` e `callout`);
- o rodapé na faixa inferior;
- schema `1.16.0`.

As três tarefas finais exigiram três passos de undo e três de redo. Os snapshots anterior e terminal foram recuperados com equivalência exata.

O botão Imprimir/PDF foi acionado e um PDF A4 com background foi gerado. As evidências incluem:

- `constructed-full.png`;
- `constructed-canvas.png`;
- `reimported-full.png`;
- `reimported-canvas.png`;
- `print-preview.png`;
- `promotional-generalization.pdf`;
- `promotional-generalization-document.json`;
- `promotional-generalization-report.json`.

## Custo de interação

Das 219 ações:

- 55 foram edições de propriedades;
- 44 foram alterações manuais de `x`, `y`, largura ou altura;
- 30 foram seleções explícitas;
- 13 foram trocas de aba;
- 11 foram inserções;
- seis foram navegações de contexto;
- seis foram operações de histórico;
- quatro participaram de exportação/importação.

A geometria manual correspondeu a aproximadamente 20% das ações. O resultado é alcançável, porém o custo ainda é alto para uma página promocional de uma única página.

## Achados válidos

### 1. Custo elevado de geometria

Quarenta e quatro ações foram usadas apenas para informar frames. A interface exige quatro alterações separadas para posicionar e dimensionar cada bloco principal.

### 2. Hierarquia visual limitada

A composição comunica a estrutura, mas não alcança nativamente alguns recursos da referência:

- faixas diagonais;
- formas de explosão promocional;
- personagem recortado;
- megafone;
- tipografia extremamente inclinada/condensada.

Esses elementos devem continuar sendo tratados como assets ou composições de arte, não como justificativa automática para novos tipos de componente.

Também foram observados:

- benefícios inferiores com rótulos pequenos e suscetíveis a truncamento;
- callout funcional, porém estreito e menos dominante que a referência;
- `section-heading` semanticamente adequado, mas com contraste promocional mais contido.

### 3. Uma referência ausente na publicação

O relatório final indicou `missingReferences: 1`, apesar de o modelo, DOM, exportação e reimportação permanecerem coerentes. A referência precisa ser identificada antes de classificar o achado como problema de conteúdo, placeholder ou validador.

### 4. Assets deliberadamente ausentes

O único finding emitido pelo benchmark corrigido foi `PLACEHOLDER_ASSETS`, com três artes sem arquivo no runner. Esse resultado é esperado pela política desta execução.

## Correção do falso positivo de seleção

O diagnóstico DB-05.20.2 demonstrou que `specification` e `icon` não eram interceptados por arte ou z-index. O diálogo já estava aberto após um clique intencional no placeholder de arte; como a arte já era a seleção corrente, o helper antigo encerrou cedo e deixou o modal ativo.

O runtime não foi alterado. O benchmark passou a:

- selecionar `art` pela árvore de Camadas quando a intenção é editar propriedades;
- reservar o clique no placeholder para escolher ou substituir asset;
- detectar qualquer diálogo residual antes de uma seleção não-art.

## Decisão

O editor demonstrou **generalização estrutural** além da referência original:

- a página pode ser criada sem código;
- os 16 tipos atuais e as receitas existentes foram suficientes;
- não houve necessidade de promover `fact`, `section-heading` ou `callout`;
- exportação, reimportação, histórico e PDF permaneceram funcionais;
- não surgiram colisões ou overflow no documento final.

O próximo ciclo deve priorizar a identificação da referência ausente e a redução do custo de geometria, não a expansão indiscriminada da biblioteca.
