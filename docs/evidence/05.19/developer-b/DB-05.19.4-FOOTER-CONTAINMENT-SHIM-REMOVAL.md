# DB-05.19.4 — Remoção do shim de contenção do `footer-item`

## Remoção física

Após a aprovação do DB-05.19.3, foram removidos:

- `app/footer-item-containment-contract.js`;
- `authoring-kit/runtime/footer-item-containment-contract.js`;
- carregamento e instalação no bootstrap do editor;
- carregamento e instalação no compilador autocontido;
- referência no build Developer B.

## Regressões desacopladas

Os testes passaram a verificar diretamente:

- geometria no registro canônico;
- recorte no CSS principal;
- ausência do shim no editor e no AuthoringKit;
- ausência de referências no build, compilador e bundle;
- paridade do registro entre editor e runtime do kit.

O executor geral e o estresse de domínio também deixaram de carregar o global removido.

## Validação anterior ao push

A remoção foi enviada somente depois de passar:

- auditoria completa de contratos Node, build e Chromium;
- teste de estresse de domínio;
- semente visual `5194001`;
- verificação de ausência de referências executáveis;
- segundo build sem diferenças.

## Commit

`fc6a47fc8305f90fd9400682ce7935c42d9db5fb` — `05.19 — remove shim de contenção do footer-item`.

## Gate estável

O workflow somente leitura do ciclo 05.19 executa sobre o head real e bloqueia o retorno dos shims de overrides tabulares e contenção do rodapé. O run associado à restauração desse gate é `29846723145`.
